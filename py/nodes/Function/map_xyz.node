import fab

title("Map (xyz)")

input('shape', fab.types.Shape)
input('Xfn', str, "=X * 2;")
input('Yfn', str, "=Y * 2;")
input('Zfn', str, "_")

output("transformed'", shape.map(fab.types.Transform(Xfn, Yfn, Zfn, '', '', '')))
